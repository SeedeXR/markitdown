[package]
name = "markitdown-cli"
description = "Self-contained CLI for converting files to Markdown (Rust port of Microsoft MarkItDown)"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[[bin]]
name = "markitdown"
path = "src/main.rs"

[features]
# Enable the fast PDFium PDF backend (needs the PDFium lib at runtime).
pdfium = ["markitdown-core/pdfium"]

[dependencies]
markitdown-core = { workspace = true }
clap = { workspace = true }
rayon = { workspace = true }
anyhow = { workspace = true }

[build-dependencies]
clap = { workspace = true }
clap_mangen = { workspace = true }
